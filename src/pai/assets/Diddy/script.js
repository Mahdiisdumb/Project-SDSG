document.getElementById("r").onclick = function () {
    location.href = "./R/run.html"; // Redirects to the parent directory and then to load.html
};

document.getElementById("c").onclick = function () {
    location.href = "./C/run.html"; // Redirects to the parent directory and then to load.html
};

document.getElementById("b").onclick = function () {
    location.href = "./B/run.html"; // Redirects to the parent directory and then to load.html
};
